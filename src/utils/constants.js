/**
 * Fallback image shown when a food/product image fails to load.
 * Used via onError handlers on all <img> tags across the app.
 */
export const FALLBACK_IMAGE_URL =
  'https://via.placeholder.com/400x300.png?text=No+Image'
