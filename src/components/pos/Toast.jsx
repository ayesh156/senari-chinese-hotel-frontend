import { toast } from 'react-toastify';

/**
 * Deprecated — use react-toastify directly: toast.success(msg), toast.error(msg)
 * Kept as re-export for backwards compatibility.
 */
export { toast };
export default toast;